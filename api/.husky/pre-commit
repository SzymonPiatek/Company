#!/bin/sh
. "$(dirname "$0")/_/husky.sh"

echo "Uruchamianie testów przez test-manager..."
docker compose run --rm test-manager npm run test:all

status=$?
if [ $status -ne 0 ]; then
  echo "Testy nie przeszły. Commit zablokowany."
  exit 1
fi

echo "Testy OK. Kontynuuję commit."
